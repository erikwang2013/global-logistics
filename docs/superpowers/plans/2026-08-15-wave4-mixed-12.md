# Wave 4: 国内二线 4（速尔/信丰/联昊通/日日顺）+ 国际 8（Delhivery/InPost/Omniva/Posti/Bring/奥地利邮政/泰国邮政/中华邮政）Implementation Plan

## 目标

在 57 家基础上再接入 12 家（国内 4 + 国际 8），共 69 家。

## 架构（沿用既有模式，见 Evri.php / Yodel.php / Uc.php 模板）

- 每适配器 = `final class X implements CarrierInterface`，命名空间 `GlobalLogistics\Carriers\{Domestic|International}`
- 构造器 `__construct(Config $config, ClientInterface $http)`（readonly 提升属性）；OAuth2 承运商在构造器内用 `OAuthTokenClient` 包装（见 Yodel.php）
- `ENDPOINT` const + `config->get('xx.endpoint', self::ENDPOINT)` 覆盖；认证密钥经 `config->get('xx.key')` 读取
- `STATUS_MAP` 关键词（小写，`|` 分隔同义）=> `TrackStatus`；`mapEvent`/`mapStatus`/`parseTime` 私有方法
- 错误契约：401/403 → `AuthException('[XX 401] 认证失败')`；>=400 → `LogisticsException('[XX 404] 接口错误')`；响应解析失败 → `LogisticsException('[XX] 响应解析失败')`；无事件 → `TrackingNotFoundException($trackingNo)`
- 事件升序排序（首尾比较降序则反转）；`deliveredAt` 仅当最新状态为 DELIVERED
- `createOrder`/`createLabel`/`subscribe` 抛 `LogisticsException('xx xxx 待实现')`
- docblock 标注 `VERIFIED-REQUIRED` + 公开文档来源链接
- 测试：每承运商 7 个用例（happy path 含 URL/方法/请求头断言、降序反转、无事件、业务错误、认证错误、非法 JSON/XML、未实现方法），fixtures 在 `tests/fixtures/{slug}/`（track/empty/error，JSON 或 XML 按承运商响应格式）
- 国内 EDI 风格承运商可参照 Uc.php（SignUtil 等），XML 响应参照 CanadaPost.php

## 承运商清单（3 个并行代理，每代理 4 家，文件互不重叠）

| 代理 | 承运商 | 代码 | 类名 |
|---|---|---|---|
| A | 速尔 | sure | Sure |
| A | 信丰 | xf | Xf |
| A | 联昊通 | lht | Lht |
| A | 日日顺 | rrs | Rrs |
| B | Delhivery（印度） | delhivery | Delhivery |
| B | InPost（波兰包裹柜） | inpost | InPost |
| B | Omniva（爱沙尼亚） | omniva | Omniva |
| B | Posti（芬兰） | posti | Posti |
| C | Bring（挪威） | bring | Bring |
| C | 奥地利邮政 | austrian-post | AustrianPost |
| C | 泰国邮政 | thailand-post | ThailandPost |
| C | 中华邮政（台湾） | chunghwa-post | ChunghwaPost |

## 共享文件（由主会话统一注册，代理不得修改）

- `src/Resources/carrier-registry.php`
- `src/Resources/detector-rules.php`（代理只报告建议正则，含冲突分析）
- `config/logistics.php`
- `tests/Unit/DetectorTest.php`
- `README.md`

## 检测规则冲突预判（主会话注册时处理）

- S10 国家码规则（AT 奥地利 / NO 挪威 Bring / TH 泰国 / TW 中华邮政 / EE 爱沙尼亚 / FI 芬兰）必须排在通用 FedEx 规则之前
- Delhivery 15 位纯数字（新增位数，与 13/14/16 互斥）；InPost 24 位纯数字（新增位数）
- 国内 4 家（速尔/信丰/联昊通/日日顺）单号若为 12 位纯数字将与 uc 冲突、13 位将与 zto 冲突，需研究位数/前缀判别；无法判别则报规则建议、由主会话决定是否仅显式调用

## 验证

- 每代理对其承运商跑 `vendor/bin/phpunit tests/Carriers/XTest.php`
- 主会话注册后跑全量 `vendor/bin/phpunit`（预计 ~540 tests）
- 57 → 69 家冒烟实例化测试
