# Wave 7: 国内二线 6 + 国际 S10 欧洲小国邮政 15 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 接入 21 家新承运商（国内 6 + 国际 15），注册表 111 → 132 家，全量测试通过并提交。

**Architecture:** 完全沿用 Wave 6 模式：每承运商一个 `CarrierInterface` 适配器（国内 6 家走快递鸟 KDN 协议、国际 15 家走 S10 欧洲小国邮政官方 API/页面解析），3 个并行代理各 7 家（文件互不重叠），主会话统一注册共享文件后跑全量测试并提交。

**Tech Stack:** PHP 8.2+、PSR-4、PHPUnit 10.5（当前 884 tests）、Guzzle PSR-18、无框架绑定。

---

## 代理工作说明书（3 个实施代理必须全部遵守）

### 适配器模板

- `final class X implements CarrierInterface`，命名空间 `GlobalLogistics\Carriers\{Domestic|International}`
- 构造器 `__construct(private readonly Config $config, private readonly ClientInterface $http)`（readonly 提升）；本波无 OAuth2 承运商
- `private const ENDPOINT` + `$this->config->get('{slug}.endpoint', self::ENDPOINT)` 覆盖；认证密钥经 `$this->config->get('{slug}.key')` 读取
- `queryTrack(string $trackingNo, array $options = []): Tracking` 实现轨迹查询；`createOrder`/`createLabel`/`subscribe` 抛 `LogisticsException('{code} createOrder 待实现')`（**代码小写**，如 `'slovak-post createOrder 待实现'`）
- **国内 6 家（KDN 协议）参照 `src/Carriers/Domestic/Fengwang.php`**：POST `{endpoint}/track?RequestType=1002`，`RequestData` 为 JSON 字符串，请求头 `DataDigest: base64(md5(RequestData+AppKey, true))`、`EBusinessID`、`Sign`；配置键 `ebusiness_id`/`app_key`/`customer_name`
- **国际 15 家（S10 邮政）参照 `src/Carriers/International/PostNord.php` 与 `src/Carriers/International/Ctt.php`**（一家带查询参数一家带复杂头，各官方 API 差异自行核实）
- **页面解析 5 家（moldova-post/albania-post/belarus-post/macedonia-post/bosnia-post）参照 `src/Carriers/International/IsraelPost.php` 先例**（HTML 表格解析或页面内嵌 JSON；无官方 API 才用页面解析，有 JSON 接口优先 JSON）
- `STATUS_MAP`：小写关键词（`|` 分隔同义）=> `TrackStatus`；`mapStatus(string): TrackStatus` 未命中兜底 `UNKNOWN`；`mapEvent` 解析单条事件（occurredAt/location/description/status）；`parseTime` 处理多格式时间戳
- **parseTime 纯日期格式一律 `!` 前缀**（如 `'!Y-m-d'`/`'!d/m/Y'`/`'!d-m-Y'`），否则 PHP 8.2+ 会把未指定时间部分填成当前时刻（wave6 踩坑修复，commit f99e1e7）
- 事件按 occurredAt 升序排序（若首事件 > 末事件则 `array_reverse`，即 `ensureAscending` 模式）；`deliveredAt` 仅当最新状态为 DELIVERED
- docblock 标注 `VERIFIED-REQUIRED:` + 公开官方文档 URL（状态映射依据，**禁止编造**；查不到的字段用默认 `IN_TRANSIT` 兜底并在汇报中说明）

### 错误契约（逐字，XX = 大写承运商代码）

- HTTP 401/403 → `AuthException('[XX 401] 认证失败')`
- HTTP >=400 → `LogisticsException('[XX 404] 接口错误')`（code 用实际状态码）
- 响应解析失败（非数组/非法 JSON/XML）→ `LogisticsException('[XX] 响应解析失败')`
- 无事件 → `TrackingNotFoundException($trackingNo)`

### 测试模板（每承运商 7 个用例，参照 `tests/Carriers/FengwangTest.php`（KDN）、`tests/Carriers/PostNordTest.php`（S10 API）或 `tests/Carriers/IsraelPostTest.php`（页面解析））

1. `testQueryTrackParses`：happy path，断言请求 URL、方法、请求头（签名/认证）、状态映射、事件升序、deliveredAt
2. `testDescendingTracesAreReversed`：内联 mock 返回降序 fixture，断言升序
3. `testEmptyTracesThrowsTrackingNotFoundException`：`tests/fixtures/{slug}/empty.json`
4. `testBusinessErrorThrowsLogisticsException`：`tests/fixtures/{slug}/error.json` + 400 响应
5. `testAuthFailuresThrowAuthException`：401（无认证概念的承运商用 403 或响应体含错误码）
6. `testNonArrayBodyThrowsLogisticsException`：`'"boom"'` 响应
7. `testUnimplementedMethodsThrow`：createOrder/createLabel/subscribe 三个断言

fixtures：`tests/fixtures/{slug}/{track,empty,error}.json`（`FakeHttpClient` 要求响应带 Content-Type 头，fixture 内容为合成数据）。

### 交付要求

- **不修改共享文件**：`src/Resources/carrier-registry.php`、`src/Resources/detector-rules.php`、`config/logistics.php`、`tests/Unit/DetectorTest.php`、`tests/Unit/CarrierRegistrySmokeTest.php`、`README.md`
- 不 commit、不 push
- **代理 A 额外报告**：国内 6 家建议检测正则 + 与现有规则的冲突分析（12 位 uc、13 位 zto、3 开头 13 位 zjs、6 开头 12 位 dainiao、77 开头 sto 等；预期全部冲突 → 倾向仅显式调用，主会话定夺）
- 自检：`vendor/bin/phpunit tests/Carriers/{X}Test.php` 全绿（每家 7 用例）
- 汇报格式：状态（DONE / BLOCKED / NEEDS_CONTEXT / DONE_WITH_CONCERNS）+ 每家 `config->get` 点号键清单 + 检测规则建议与冲突分析 + 官方文档 URL

---

### Task 1: 基线验证

- [ ] **Step 1: 跑全量测试确认基线**

Run: `vendor/bin/phpunit`
Expected: `OK (884 tests, ...)`（绿色）

- [ ] **Step 2: 确认冒烟断言为 111**

Run: `grep -n "assertSame(111" tests/Unit/CarrierRegistrySmokeTest.php`
Expected: `$this->assertSame(111, $count);`

### Task 2: 代理 A — 国内 6（KDN）+ Slovak Post（SK）

**Files（全部新建）：**
- Create: `src/Carriers/Domestic/{Zhongyou,Huitong,Quanfeng,Guotong,Yuancheng,Xinbang}.php`
- Create: `tests/Carriers/{Zhongyou,Huitong,Quanfeng,Guotong,Yuancheng,Xinbang}Test.php`
- Create: `src/Carriers/International/SlovakPost.php`、`tests/Carriers/SlovakPostTest.php`
- Create: `tests/fixtures/{zhongyou,huitong,quanfeng,guotong,yuancheng,xinbang,slovak-post}/`

- [ ] **Step 1: 派发实施代理 A**（背景运行，7 家，文件集合与代理 B/C 不重叠）

代理 A 任务书：
1. 按「代理工作说明书」实现 7 个适配器：中邮物流 `zhongyou`/Zhongyou、汇通快运 `huitong`/Huitong、全峰快递 `quanfeng`/Quanfeng、国通快递 `guotong`/Guotong、远成快运 `yuancheng`/Yuancheng、新邦物流 `xinbang`/Xinbang（6 家 KDN 参照 Fengwang.php）、Slovak Post 斯洛伐克 `slovak-post`/SlovakPost（S10 SK 参照 PostNord.php）
2. 每家用官方文档核实 KDN 覆盖与单号格式（`VERIFIED-REQUIRED`）；若某家 KDN 无官方文档，替换为备选（如 增益速递 zengyi / 京广速递 jingguang）并在汇报中说明
3. 报告 6 家国内的建议检测正则 + 冲突分析（预期与 12/13 位数字规则冲突 → 倾向仅显式调用，主会话定夺）
4. 自检 7 个测试文件全绿后返回 DONE

- [ ] **Step 2: 验证代理 A 输出**

Run: `vendor/bin/phpunit tests/Carriers/ZhongyouTest.php tests/Carriers/HuitongTest.php tests/Carriers/QuanfengTest.php tests/Carriers/GuotongTest.php tests/Carriers/YuanchengTest.php tests/Carriers/XinbangTest.php tests/Carriers/SlovakPostTest.php`
Expected: `OK (49 tests, ...)` 全绿；抽查 1-2 家适配器核对错误契约、`ensureAscending` 与 parseTime `!` 前缀

### Task 3: 代理 B — S10 邮政 7（SI/RS/BG/LT/LV/IS/MT）

**Files（全部新建）：**
- Create: `src/Carriers/International/{SloveniaPost,SerbiaPost,BulgariaPost,LithuaniaPost,LatviaPost,IcelandPost,MaltaPost}.php`
- Create: `tests/Carriers/{SloveniaPost,SerbiaPost,BulgariaPost,LithuaniaPost,LatviaPost,IcelandPost,MaltaPost}Test.php`
- Create: `tests/fixtures/{slovenia-post,serbia-post,bulgaria-post,lithuania-post,latvia-post,iceland-post,malta-post}/`

- [ ] **Step 1: 派发实施代理 B**（背景运行，7 家）

代理 B 任务书：
1. 按「代理工作说明书」实现 7 个适配器：Pošta Slovenije 斯洛文尼亚 `slovenia-post`/SloveniaPost（S10 SI）、Pošta Srbije 塞尔维亚 `serbia-post`/SerbiaPost（RS）、Bulgarian Posts 保加利亚 `bulgaria-post`/BulgariaPost（BG）、Lietuvos paštas 立陶宛 `lithuania-post`/LithuaniaPost（LT）、Latvijas Pasts 拉脱维亚 `latvia-post`/LatviaPost（LV）、Íslandspóstur 冰岛 `iceland-post`/IcelandPost（IS）、MaltaPost 马耳他 `malta-post`/MaltaPost（MT）
2. 每家官方 API 差异自行核实（auth 头、查询参数、JSON/XML），`VERIFIED-REQUIRED`
3. 自检 7 个测试文件全绿后返回 DONE

- [ ] **Step 2: 验证代理 B 输出**

Run: `vendor/bin/phpunit tests/Carriers/SloveniaPostTest.php tests/Carriers/SerbiaPostTest.php tests/Carriers/BulgariaPostTest.php tests/Carriers/LithuaniaPostTest.php tests/Carriers/LatviaPostTest.php tests/Carriers/IcelandPostTest.php tests/Carriers/MaltaPostTest.php`
Expected: `OK (49 tests, ...)` 全绿

### Task 4: 代理 C — S10 邮政 7（LU/CY/MD/AL/BY/MK/BA）

**Files（全部新建）：**
- Create: `src/Carriers/International/{LuxembourgPost,CyprusPost,MoldovaPost,AlbaniaPost,BelarusPost,MacedoniaPost,BosniaPost}.php`
- Create: `tests/Carriers/{LuxembourgPost,CyprusPost,MoldovaPost,AlbaniaPost,BelarusPost,MacedoniaPost,BosniaPost}Test.php`
- Create: `tests/fixtures/{luxembourg-post,cyprus-post,moldova-post,albania-post,belarus-post,macedonia-post,bosnia-post}/`

- [ ] **Step 1: 派发实施代理 C**（背景运行，7 家）

代理 C 任务书：
1. 按「代理工作说明书」实现 7 个适配器：POST Luxembourg 卢森堡 `luxembourg-post`/LuxembourgPost（S10 LU）、Cyprus Post 塞浦路斯 `cyprus-post`/CyprusPost（CY）、Poșta Moldovei 摩尔多瓦 `moldova-post`/MoldovaPost（MD）、Posta Shqiptare 阿尔巴尼亚 `albania-post`/AlbaniaPost（AL）、Belpochta 白俄罗斯 `belarus-post`/BelarusPost（BY）、Makedonska Pošta 北马其顿 `macedonia-post`/MacedoniaPost（MK）、BH Pošta 波黑 `bosnia-post`/BosniaPost（BA）
2. MD/AL/BY/MK/BA 五家无公开 JSON API 时按 IsraelPost 页面解析先例实现（有 JSON 接口优先 JSON）；其余官方 API 差异自行核实，`VERIFIED-REQUIRED`
3. 自检 7 个测试文件全绿后返回 DONE

- [ ] **Step 2: 验证代理 C 输出**

Run: `vendor/bin/phpunit tests/Carriers/LuxembourgPostTest.php tests/Carriers/CyprusPostTest.php tests/Carriers/MoldovaPostTest.php tests/Carriers/AlbaniaPostTest.php tests/Carriers/BelarusPostTest.php tests/Carriers/MacedoniaPostTest.php tests/Carriers/BosniaPostTest.php`
Expected: `OK (49 tests, ...)` 全绿

### Task 5: 主会话注册共享文件

- [ ] **Step 1: 收集 config 键并写入 `config/logistics.php`**

Run: `grep -oh "config->get('[a-z-]*\.[a-z_]*" src/Carriers/Domestic/{Zhongyou,Huitong,Quanfeng,Guotong,Yuancheng,Xinbang}.php src/Carriers/International/{SlovakPost,SloveniaPost,SerbiaPost,BulgariaPost,LithuaniaPost,LatviaPost,IcelandPost,MaltaPost,LuxembourgPost,CyprusPost,MoldovaPost,AlbaniaPost,BelarusPost,MacedoniaPost,BosniaPost}.php | sed "s/config->get('//" | sort -u`
按结果在 `config/logistics.php` 为 21 家各加一行注释键数组（如 `'slovak-post' => ['endpoint' => '']`），国内 6 家沿用 `ebusiness_id`/`app_key`/`customer_name`/`endpoint` 结构

- [ ] **Step 2: 注册 `src/Resources/carrier-registry.php`**

- use 语句（国内块末尾，即 `use GlobalLogistics\Carriers\Domestic\Zhongtie;` 之后追加，内部字母序）：`Guotong, Huitong, Quanfeng, Xinbang, Yuancheng, Zhongyou`
- use 语句（国际块末尾，即 `use GlobalLogistics\Carriers\International\Ukrposhta;` 之后追加，内部字母序）：`AlbaniaPost, BelarusPost, BosniaPost, BulgariaPost, CyprusPost, IcelandPost, LatviaPost, LithuaniaPost, LuxembourgPost, MacedoniaPost, MaltaPost, MoldovaPost, SerbiaPost, SlovakPost, SloveniaPost`
- 国内条目（`'cae' => Cae::class,` 之后追加）：`'zhongyou' => Zhongyou::class, 'huitong' => Huitong::class, 'quanfeng' => Quanfeng::class, 'guotong' => Guotong::class, 'yuancheng' => Yuancheng::class, 'xinbang' => Xinbang::class,`
- 国际条目（`'croatia-post' => CroatiaPost::class,` 之后追加）：`'slovak-post' => SlovakPost::class, 'slovenia-post' => SloveniaPost::class, 'serbia-post' => SerbiaPost::class, 'bulgaria-post' => BulgariaPost::class, 'lithuania-post' => LithuaniaPost::class, 'latvia-post' => LatviaPost::class, 'iceland-post' => IcelandPost::class, 'malta-post' => MaltaPost::class, 'luxembourg-post' => LuxembourgPost::class, 'cyprus-post' => CyprusPost::class, 'moldova-post' => MoldovaPost::class, 'albania-post' => AlbaniaPost::class, 'belarus-post' => BelarusPost::class, 'macedonia-post' => MacedoniaPost::class, 'bosnia-post' => BosniaPost::class,`

- [ ] **Step 3: 检测规则 `src/Resources/detector-rules.php`**

在 `/^[A-Z]{2}\d{9}HR$/i`（croatia-post）与燕文规则 `/^[A-Z]{2}\d{9}(YP|YW|YE|YL)$/i` 之间插入 15 条（国家码互不重复，无相互冲突；全部在通用 FedEx 规则之前）：

```php
'/^[A-Z]{2}\d{9}SK$/i' => ['international', 'slovak-post'], // 斯洛伐克 Slovak Post，同上
'/^[A-Z]{2}\d{9}SI$/i' => ['international', 'slovenia-post'], // 斯洛文尼亚 Pošta Slovenije，同上
'/^[A-Z]{2}\d{9}RS$/i' => ['international', 'serbia-post'], // 塞尔维亚 Pošta Srbije，同上
'/^[A-Z]{2}\d{9}BG$/i' => ['international', 'bulgaria-post'], // 保加利亚 Bulgarian Posts，同上
'/^[A-Z]{2}\d{9}LT$/i' => ['international', 'lithuania-post'], // 立陶宛 Lietuvos paštas，同上
'/^[A-Z]{2}\d{9}LV$/i' => ['international', 'latvia-post'], // 拉脱维亚 Latvijas Pasts，同上
'/^[A-Z]{2}\d{9}MD$/i' => ['international', 'moldova-post'], // 摩尔多瓦 Poșta Moldovei，同上
'/^[A-Z]{2}\d{9}AL$/i' => ['international', 'albania-post'], // 阿尔巴尼亚 Posta Shqiptare，同上
'/^[A-Z]{2}\d{9}MT$/i' => ['international', 'malta-post'], // 马耳他 MaltaPost，同上
'/^[A-Z]{2}\d{9}LU$/i' => ['international', 'luxembourg-post'], // 卢森堡 POST Luxembourg，同上
'/^[A-Z]{2}\d{9}IS$/i' => ['international', 'iceland-post'], // 冰岛 Íslandspóstur，同上
'/^[A-Z]{2}\d{9}CY$/i' => ['international', 'cyprus-post'], // 塞浦路斯 Cyprus Post，同上
'/^[A-Z]{2}\d{9}BY$/i' => ['international', 'belarus-post'], // 白俄罗斯 Belpochta，同上
'/^[A-Z]{2}\d{9}MK$/i' => ['international', 'macedonia-post'], // 北马其顿 Makedonska Pošta，同上
'/^[A-Z]{2}\d{9}BA$/i' => ['international', 'bosnia-post'], // 波黑 BH Pošta，同上
```

国内 6 家按代理 A 的冲突分析定夺：若建议正则与现有规则冲突（预期如此），按 Wave 4/5/6 决策**仅显式调用，不注册规则**；若代理给出无冲突判别式则注册。

- [ ] **Step 4: `tests/Unit/DetectorTest.php` 新增 15 个用例**

在 `testDetectsPostiJjfiPrefix` 之前插入（样式同现有 S10 用例，`Detector::withDefaults()` + `detect()` + 断言 `Channel::International` 与承运商代码）：

| 测试方法 | 样例单号 | 期望代码 |
|---|---|---|
| testDetectsSlovakPostBeforeGenericFedExRule | RA123456789SK | slovak-post |
| testDetectsSloveniaPostBeforeGenericFedExRule | RA123456789SI | slovenia-post |
| testDetectsSerbiaPostBeforeGenericFedExRule | RA123456789RS | serbia-post |
| testDetectsBulgariaPostBeforeGenericFedExRule | RA123456789BG | bulgaria-post |
| testDetectsLithuaniaPostBeforeGenericFedExRule | RA123456789LT | lithuania-post |
| testDetectsLatviaPostBeforeGenericFedExRule | RA123456789LV | latvia-post |
| testDetectsMoldovaPostBeforeGenericFedExRule | RA123456789MD | moldova-post |
| testDetectsAlbaniaPostBeforeGenericFedExRule | RA123456789AL | albania-post |
| testDetectsMaltaPostBeforeGenericFedExRule | RA123456789MT | malta-post |
| testDetectsLuxembourgPostBeforeGenericFedExRule | RA123456789LU | luxembourg-post |
| testDetectsIcelandPostBeforeGenericFedExRule | RA123456789IS | iceland-post |
| testDetectsCyprusPostBeforeGenericFedExRule | RA123456789CY | cyprus-post |
| testDetectsBelarusPostBeforeGenericFedExRule | RA123456789BY | belarus-post |
| testDetectsMacedoniaPostBeforeGenericFedExRule | RA123456789MK | macedonia-post |
| testDetectsBosniaPostBeforeGenericFedExRule | RA123456789BA | bosnia-post |

- [ ] **Step 5: 冒烟断言 111 → 132**

Modify: `tests/Unit/CarrierRegistrySmokeTest.php:31` → `$this->assertSame(132, $count);`

- [ ] **Step 6: `README.md`**

- 「已接入承运商（111 家）」→ 132 家；「94 条正则规则」→ 109
- 国内表追加 6 行：中邮物流 `zhongyou` / 汇通快运 `huitong` / 全峰快递 `quanfeng` / 国通快递 `guotong` / 远成快运 `yuancheng` / 新邦物流 `xinbang`
- 国际表追加 15 行：Slovak Post（斯洛伐克）`slovak-post` / Pošta Slovenije（斯洛文尼亚）`slovenia-post` / Pošta Srbije（塞尔维亚）`serbia-post` / Bulgarian Posts（保加利亚）`bulgaria-post` / Lietuvos paštas（立陶宛）`lithuania-post` / Latvijas Pasts（拉脱维亚）`latvia-post` / Íslandspóstur（冰岛）`iceland-post` / MaltaPost（马耳他）`malta-post` / POST Luxembourg（卢森堡）`luxembourg-post` / Cyprus Post（塞浦路斯）`cyprus-post` / Poșta Moldovei（摩尔多瓦）`moldova-post` / Posta Shqiptare（阿尔巴尼亚）`albania-post` / Belpochta（白俄罗斯）`belarus-post` / Makedonska Pošta（北马其顿）`macedonia-post` / BH Pošta（波黑）`bosnia-post`

- [ ] **Step 7: 局部验证**

Run: `vendor/bin/phpunit tests/Unit/DetectorTest.php tests/Unit/CarrierRegistrySmokeTest.php`
Expected: `OK (108 tests, ...)`（DetectorTest 107 + 冒烟 1）

### Task 6: 全量验证 + 提交

- [ ] **Step 1: 全量测试**

Run: `vendor/bin/phpunit`
Expected: `OK (1046 tests, ...)`（884 基线 + 147 新适配器 + 15 新检测用例），全绿

- [ ] **Step 2: 提交**

```bash
git add README.md config/logistics.php src/Resources/carrier-registry.php src/Resources/detector-rules.php tests/Unit/CarrierRegistrySmokeTest.php tests/Unit/DetectorTest.php docs/superpowers/plans/2026-08-15-wave7-mixed-21.md src/Carriers/Domestic/ src/Carriers/International/ tests/Carriers/ tests/fixtures/
git commit -m "$(cat <<'EOF'
feat: 接入 21 家新承运商（国内 6 + 国际 15），共 132 家

国内：中邮物流、汇通快运、全峰快递、国通快递、远成快运、新邦物流
国际：Slovak Post、Pošta Slovenije、Pošta Srbije、Bulgarian Posts、
Lietuvos paštas、Latvijas Pasts、Íslandspóstur、MaltaPost、
POST Luxembourg、Cyprus Post、Poșta Moldovei、Posta Shqiptare、
Belpochta、Makedonska Pošta、BH Pošta
EOF
)"
```

- [ ] **Step 3: 验证工作区干净**

Run: `git status --short`
Expected: 空输出（无未提交/未跟踪文件）

## 自检

- 规格覆盖：21 家清单（Task 2/3/4）✓、共享文件注册（Task 5）✓、S10 规则 15 条置于 FedEx 规则前（Step 3）✓、国内 6 家仅显式调用决策（Step 3 注）✓、冒烟 132（Step 5）✓、提交信息（Task 6）✓
- 占位符扫描：无 TBD/TODO；每家适配器/测试/夹具路径精确给出
- 类型一致性：类名/代码/国家码三表（规格、Task 2/3/4、Task 5）逐一核对一致；`{slug}` 与测试类名、fixtures 目录名一致；S10 正则与国家码一致；15 个国家码（SK/SI/RS/BG/LT/LV/MD/AL/MT/LU/IS/CY/BY/MK/BA）与现有 47 个国家码规则逐一比对无重复（已核对现有：CN GB JP HK BR KR FR NZ IT RU SG CH BE ES AT NO TH TW EE FI PT IE PL IN MY AE HU CZ GR VN UA TR IL EG SA ZA MX AR CL ID PH PK KZ RO HR）
