# Wave 6: 国内二线 6 + 国际 S10 邮政 15 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 接入 21 家新承运商（国内 6 + 国际 15），注册表 90 → 111 家，全量测试通过并提交。

**Architecture:** 完全沿用 Wave 5 模式：每承运商一个 `CarrierInterface` 适配器（国内 6 家走快递鸟 KDN 协议、国际 15 家走 S10 邮政官方 API），3 个并行代理各 7 家（文件互不重叠），主会话统一注册共享文件后跑全量测试并提交。

**Tech Stack:** PHP 8.2+、PSR-4、PHPUnit 10.5（当前 722 tests）、Guzzle PSR-18、无框架绑定。

---

## 代理工作说明书（3 个实施代理必须全部遵守）

### 适配器模板

- `final class X implements CarrierInterface`，命名空间 `GlobalLogistics\Carriers\{Domestic|International}`
- 构造器 `__construct(private readonly Config $config, private readonly ClientInterface $http)`（readonly 提升）；本波无 OAuth2 承运商
- `private const ENDPOINT` + `$this->config->get('{slug}.endpoint', self::ENDPOINT)` 覆盖；认证密钥经 `$this->config->get('{slug}.key')` 读取
- `queryTrack(string $trackingNo, array $options = []): Tracking` 实现轨迹查询；`createOrder`/`createLabel`/`subscribe` 抛 `LogisticsException('{code} createOrder 待实现')`（**代码小写**，如 `'ukrposhta createOrder 待实现'`）
- **国内 6 家（KDN 协议）参照 `src/Carriers/Domestic/Fengwang.php`**：POST `{endpoint}/track?RequestType=1002`，`RequestData` 为 JSON 字符串，请求头 `DataDigest: base64(md5(RequestData+AppKey, true))`、`EBusinessID`、`Sign`；配置键 `ebusiness_id`/`app_key`/`customer_name`
- **国际 15 家（S10 邮政）参照 `src/Carriers/International/PostNord.php` 与 `src/Carriers/International/Ctt.php`**（一家带查询参数一家带复杂头，各官方 API 差异自行核实）
- `STATUS_MAP`：小写关键词（`|` 分隔同义）=> `TrackStatus`；`mapStatus(string): TrackStatus` 未命中兜底 `UNKNOWN`；`mapEvent` 解析单条事件（occurredAt/location/description/status）；`parseTime` 处理多格式时间戳
- 事件按 occurredAt 升序排序（若首事件 > 末事件则 `array_reverse`，即 `ensureAscending` 模式）；`deliveredAt` 仅当最新状态为 DELIVERED
- docblock 标注 `VERIFIED-REQUIRED:` + 公开官方文档 URL（状态映射依据，**禁止编造**；查不到的字段用默认 `IN_TRANSIT` 兜底并在汇报中说明）

### 错误契约（逐字，XX = 大写承运商代码）

- HTTP 401/403 → `AuthException('[XX 401] 认证失败')`
- HTTP >=400 → `LogisticsException('[XX 404] 接口错误')`（code 用实际状态码）
- 响应解析失败（非数组/非法 JSON/XML）→ `LogisticsException('[XX] 响应解析失败')`
- 无事件 → `TrackingNotFoundException($trackingNo)`

### 测试模板（每承运商 7 个用例，参照 `tests/Carriers/FengwangTest.php`（KDN）或 `tests/Carriers/PostNordTest.php`（S10））

1. `testQueryTrackParses`：happy path，断言请求 URL、方法、请求头（签名/认证）、状态映射、事件升序、deliveredAt
2. `testDescendingTracesAreReversed`：内联 mock 返回降序 fixture，断言升序
3. `testEmptyTracesThrowsTrackingNotFoundException`：`tests/fixtures/{slug}/empty.json`
4. `testBusinessErrorThrowsLogisticsException`：`tests/fixtures/{slug}/error.json` + 400 响应
5. `testAuthFailuresThrowAuthException`：401（无认证概念的承运商用 403 或响应体含错误码）
6. `testNonArrayBodyThrowsLogisticsException`：`'"boom"'` 响应
7. `testUnimplementedMethodsThrow`：createOrder/createLabel/subscribe 三个断言

fixtures：`tests/fixtures/{slug}/{track,empty,error}.json`（`FakeHttpClient` 要求响应带 Content-Type 头，fixture 内容为合成数据）。

### 交付要求

- **不修改共享文件**：`src/Resources/carrier-registry.php`、`src/Resources/detector-rules.php`、`config/logistics.php`、`tests/Unit/DetectorTest.php`、`tests/Unit/CarrierRegistrySmokeTest.php`、`README.md`
- 不 commit、不 push
- **代理 A 额外报告**：国内 6 家建议检测正则 + 与现有规则的冲突分析（12 位 uc、13 位 zto、3 开头 13 位 zjs、6 开头 12 位 dainiao、77 开头 sto 等）
- 自检：`vendor/bin/phpunit tests/Carriers/{X}Test.php` 全绿（每家 7 用例）
- 汇报格式：状态（DONE / BLOCKED / NEEDS_CONTEXT / DONE_WITH_CONCERNS）+ 每家 `config->get` 点号键清单 + 检测规则建议与冲突分析 + 官方文档 URL

---

### Task 1: 基线验证

- [ ] **Step 1: 跑全量测试确认基线**

Run: `vendor/bin/phpunit`
Expected: `OK (722 tests, 3124 assertions)`（绿色）

- [ ] **Step 2: 确认冒烟断言为 90**

Run: `grep -n "assertSame(90" tests/Unit/CarrierRegistrySmokeTest.php`
Expected: `$this->assertSame(90, $count);`

### Task 2: 代理 A — 国内 6（KDN）+ Ukrposhta（UA）

**Files（全部新建）：**
- Create: `src/Carriers/Domestic/{Huayu,Jiaji,Longbang,Suteng,Zhongtie,Qy}.php`
- Create: `tests/Carriers/{Huayu,Jiaji,Longbang,Suteng,Zhongtie,Qy}Test.php`
- Create: `src/Carriers/International/Ukrposhta.php`、`tests/Carriers/UkrposhtaTest.php`
- Create: `tests/fixtures/{huayu,jiaji,longbang,suteng,zhongtie,qy,ukrposhta}/`

- [ ] **Step 1: 派发实施代理 A**（背景运行，7 家，文件集合与代理 B/C 不重叠）

代理 A 任务书：
1. 按「代理工作说明书」实现 7 个适配器：天地华宇 `huayu`/Huayu、佳吉快运 `jiaji`/Jiaji、龙邦速递 `longbang`/Longbang、速腾物流 `suteng`/Suteng、中铁物流 `zhongtie`/Zhongtie（区别于 cre）、全一快递 `qy`/Qy（6 家 KDN 参照 Fengwang.php）、Ukrposhta 乌克兰 `ukrposhta`/Ukrposhta（S10 UA 参照 PostNord.php）
2. 每家用官方文档核实 KDN 覆盖与单号格式（`VERIFIED-REQUIRED`）；若某家 KDN 无官方文档，替换为备选（中邮物流 zhongyou / 汇通快运 huitong）并在汇报中说明
3. 报告 6 家国内的建议检测正则 + 冲突分析（预期与 12/13 位数字规则冲突 → 倾向仅显式调用，主会话定夺）
4. 自检 7 个测试文件全绿后返回 DONE

- [ ] **Step 2: 验证代理 A 输出**

Run: `vendor/bin/phpunit tests/Carriers/HuayuTest.php tests/Carriers/JiajiTest.php tests/Carriers/LongbangTest.php tests/Carriers/SutengTest.php tests/Carriers/ZhongtieTest.php tests/Carriers/QyTest.php tests/Carriers/UkrposhtaTest.php`
Expected: `OK (49 tests, ...)` 全绿；抽查 1-2 家适配器核对错误契约与 `ensureAscending`

### Task 3: 代理 B — S10 邮政 7（TR/IL/EG/SA/ZA/MX/AR）

**Files（全部新建）：**
- Create: `src/Carriers/International/{TurkeyPost,IsraelPost,EgyptPost,SaudiPost,SouthAfricanPost,CorreosMexico,CorreoArgentino}.php`
- Create: `tests/Carriers/{TurkeyPost,IsraelPost,EgyptPost,SaudiPost,SouthAfricanPost,CorreosMexico,CorreoArgentino}Test.php`
- Create: `tests/fixtures/{turkey-post,israel-post,egypt-post,saudi-post,south-african-post,correos-mexico,correo-argentino}/`

> 注：规格表中 correo-argentino 原在代理 C，为均衡 7/7/7 移至代理 B（文件仍互不重叠）。

- [ ] **Step 1: 派发实施代理 B**（背景运行，7 家）

代理 B 任务书：
1. 按「代理工作说明书」实现 7 个适配器：Turkey PTT 土耳其 `turkey-post`/TurkeyPost（S10 TR）、Israel Post 以色列 `israel-post`/IsraelPost（IL）、Egypt Post 埃及 `egypt-post`/EgyptPost（EG）、Saudi Post 沙特 `saudi-post`/SaudiPost（SA）、South African Post 南非 `south-african-post`/SouthAfricanPost（ZA）、Correos de México 墨西哥 `correos-mexico`/CorreosMexico（MX）、Correo Argentino 阿根廷 `correo-argentino`/CorreoArgentino（AR）
2. 每家官方 API 差异自行核实（auth 头、查询参数、JSON/XML），`VERIFIED-REQUIRED`
3. 自检 7 个测试文件全绿后返回 DONE

- [ ] **Step 2: 验证代理 B 输出**

Run: `vendor/bin/phpunit tests/Carriers/TurkeyPostTest.php tests/Carriers/IsraelPostTest.php tests/Carriers/EgyptPostTest.php tests/Carriers/SaudiPostTest.php tests/Carriers/SouthAfricanPostTest.php tests/Carriers/CorreosMexicoTest.php tests/Carriers/CorreoArgentinoTest.php`
Expected: `OK (49 tests, ...)` 全绿

### Task 4: 代理 C — S10 邮政 7（CL/ID/PH/PK/KZ/RO/HR）

**Files（全部新建）：**
- Create: `src/Carriers/International/{CorreosChile,PosIndonesia,PhlPost,PakistanPost,Kazpost,RomaniaPost,CroatiaPost}.php`
- Create: `tests/Carriers/{CorreosChile,PosIndonesia,PhlPost,PakistanPost,Kazpost,RomaniaPost,CroatiaPost}Test.php`
- Create: `tests/fixtures/{correos-chile,pos-indonesia,phl-post,pakistan-post,kazpost,romania-post,croatia-post}/`

- [ ] **Step 1: 派发实施代理 C**（背景运行，7 家）

代理 C 任务书：
1. 按「代理工作说明书」实现 7 个适配器：Correos de Chile 智利 `correos-chile`/CorreosChile（S10 CL）、Pos Indonesia 印尼 `pos-indonesia`/PosIndonesia（ID）、PHLPost 菲律宾 `phl-post`/PhlPost（PH）、Pakistan Post 巴基斯坦 `pakistan-post`/PakistanPost（PK）、Kazpost 哈萨克斯坦 `kazpost`/Kazpost（KZ）、Poșta Română 罗马尼亚 `romania-post`/RomaniaPost（RO）、Hrvatska pošta 克罗地亚 `croatia-post`/CroatiaPost（HR）
2. 每家官方 API 差异自行核实，`VERIFIED-REQUIRED`
3. 自检 7 个测试文件全绿后返回 DONE

- [ ] **Step 2: 验证代理 C 输出**

Run: `vendor/bin/phpunit tests/Carriers/CorreosChileTest.php tests/Carriers/PosIndonesiaTest.php tests/Carriers/PhlPostTest.php tests/Carriers/PakistanPostTest.php tests/Carriers/KazpostTest.php tests/Carriers/RomaniaPostTest.php tests/Carriers/CroatiaPostTest.php`
Expected: `OK (49 tests, ...)` 全绿

### Task 5: 主会话注册共享文件

- [ ] **Step 1: 收集 config 键并写入 `config/logistics.php`**

Run: `grep -oh "config->get('[a-z-]*\.[a-z_]*" src/Carriers/Domestic/{Huayu,Jiaji,Longbang,Suteng,Zhongtie,Qy}.php src/Carriers/International/{Ukrposhta,TurkeyPost,IsraelPost,EgyptPost,SaudiPost,SouthAfricanPost,CorreosMexico,CorreoArgentino,CorreosChile,PosIndonesia,PhlPost,PakistanPost,Kazpost,RomaniaPost,CroatiaPost}.php | sed "s/config->get('//" | sort -u`
按结果在 `config/logistics.php` 为 21 家各加一行注释键数组（如 `'ukrposhta' => ['api_key' => '', 'endpoint' => '']`），国内 6 家沿用 `ebusiness_id`/`app_key`/`endpoint` 结构

- [ ] **Step 2: 注册 `src/Resources/carrier-registry.php`**

- use 语句（国内块末尾，即 `use GlobalLogistics\Carriers\Domestic\Cae;` 之后追加，内部字母序）：`Huayu, Jiaji, Longbang, Qy, Suteng, Zhongtie`
- use 语句（国际块末尾，即 `use GlobalLogistics\Carriers\International\Winit;` 之后追加，内部字母序）：`CorreoArgentino, CorreosChile, CorreosMexico, CroatiaPost, EgyptPost, IsraelPost, Kazpost, PakistanPost, PhlPost, PosIndonesia, RomaniaPost, SaudiPost, SouthAfricanPost, TurkeyPost, Ukrposhta`
- 国内条目（`'cae' => Cae::class,` 之后追加）：`'huayu' => Huayu::class, 'jiaji' => Jiaji::class, 'longbang' => Longbang::class, 'qy' => Qy::class, 'suteng' => Suteng::class, 'zhongtie' => Zhongtie::class,`
- 国际条目（`'winit' => Winit::class,` 之后追加）：`'ukrposhta' => Ukrposhta::class, 'turkey-post' => TurkeyPost::class, 'israel-post' => IsraelPost::class, 'egypt-post' => EgyptPost::class, 'saudi-post' => SaudiPost::class, 'south-african-post' => SouthAfricanPost::class, 'correos-mexico' => CorreosMexico::class, 'correo-argentino' => CorreoArgentino::class, 'correos-chile' => CorreosChile::class, 'pos-indonesia' => PosIndonesia::class, 'phl-post' => PhlPost::class, 'pakistan-post' => PakistanPost::class, 'kazpost' => Kazpost::class, 'romania-post' => RomaniaPost::class, 'croatia-post' => CroatiaPost::class,`

- [ ] **Step 3: 检测规则 `src/Resources/detector-rules.php`**

在 `/^[A-Z]{2}\d{9}VN$/i`（viettel-post）与燕文规则 `/^[A-Z]{2}\d{9}(YP|YW|YE|YL)$/i` 之间插入 15 条（国家码互不重复，无相互冲突；全部在通用 FedEx 规则之前）：

```php
'/^[A-Z]{2}\d{9}UA$/i' => ['international', 'ukrposhta'], // 乌克兰 Ukrposhta，同上
'/^[A-Z]{2}\d{9}TR$/i' => ['international', 'turkey-post'], // 土耳其 PTT，同上
'/^[A-Z]{2}\d{9}IL$/i' => ['international', 'israel-post'], // 以色列 Israel Post，同上
'/^[A-Z]{2}\d{9}EG$/i' => ['international', 'egypt-post'], // 埃及 Egypt Post，同上
'/^[A-Z]{2}\d{9}SA$/i' => ['international', 'saudi-post'], // 沙特 Saudi Post，同上
'/^[A-Z]{2}\d{9}ZA$/i' => ['international', 'south-african-post'], // 南非 South African Post，同上
'/^[A-Z]{2}\d{9}MX$/i' => ['international', 'correos-mexico'], // 墨西哥 Correos de México，同上
'/^[A-Z]{2}\d{9}AR$/i' => ['international', 'correo-argentino'], // 阿根廷 Correo Argentino，同上
'/^[A-Z]{2}\d{9}CL$/i' => ['international', 'correos-chile'], // 智利 Correos de Chile，同上
'/^[A-Z]{2}\d{9}ID$/i' => ['international', 'pos-indonesia'], // 印尼 Pos Indonesia，同上
'/^[A-Z]{2}\d{9}PH$/i' => ['international', 'phl-post'], // 菲律宾 PHLPost，同上
'/^[A-Z]{2}\d{9}PK$/i' => ['international', 'pakistan-post'], // 巴基斯坦 Pakistan Post，同上
'/^[A-Z]{2}\d{9}KZ$/i' => ['international', 'kazpost'], // 哈萨克斯坦 Kazpost，同上
'/^[A-Z]{2}\d{9}RO$/i' => ['international', 'romania-post'], // 罗马尼亚 Poșta Română，同上
'/^[A-Z]{2}\d{9}HR$/i' => ['international', 'croatia-post'], // 克罗地亚 Hrvatska pošta，同上
```

国内 6 家按代理 A 的冲突分析定夺：若建议正则与现有规则冲突（预期如此），按 Wave 4/5 决策**仅显式调用，不注册规则**；若代理给出无冲突判别式则注册。

- [ ] **Step 4: `tests/Unit/DetectorTest.php` 新增 15 个用例**

在 `testDetectsPostiJjfiPrefix` 之前插入（样式同现有 S10 用例，`Detector::withDefaults()` + `detect()` + 断言 `Channel::International` 与承运商代码）：

| 测试方法 | 样例单号 | 期望代码 |
|---|---|---|
| testDetectsUkrposhtaBeforeGenericFedExRule | RA123456789UA | ukrposhta |
| testDetectsTurkeyPostBeforeGenericFedExRule | RA123456789TR | turkey-post |
| testDetectsIsraelPostBeforeGenericFedExRule | RA123456789IL | israel-post |
| testDetectsEgyptPostBeforeGenericFedExRule | RA123456789EG | egypt-post |
| testDetectsSaudiPostBeforeGenericFedExRule | RA123456789SA | saudi-post |
| testDetectsSouthAfricanPostBeforeGenericFedExRule | RA123456789ZA | south-african-post |
| testDetectsCorreosMexicoBeforeGenericFedExRule | RA123456789MX | correos-mexico |
| testDetectsCorreoArgentinoBeforeGenericFedExRule | RA123456789AR | correo-argentino |
| testDetectsCorreosChileBeforeGenericFedExRule | RA123456789CL | correos-chile |
| testDetectsPosIndonesiaBeforeGenericFedExRule | RA123456789ID | pos-indonesia |
| testDetectsPhlPostBeforeGenericFedExRule | RA123456789PH | phl-post |
| testDetectsPakistanPostBeforeGenericFedExRule | RA123456789PK | pakistan-post |
| testDetectsKazpostBeforeGenericFedExRule | RA123456789KZ | kazpost |
| testDetectsRomaniaPostBeforeGenericFedExRule | RA123456789RO | romania-post |
| testDetectsCroatiaPostBeforeGenericFedExRule | RA123456789HR | croatia-post |

- [ ] **Step 5: 冒烟断言 90 → 111**

Modify: `tests/Unit/CarrierRegistrySmokeTest.php:31` → `$this->assertSame(111, $count);`

- [ ] **Step 6: `README.md`**

- 「已接入承运商（90 家）」→ 111 家；「79 条正则规则」→ 94
- 国内表追加 6 行：天地华宇 `huayu` / 佳吉快运 `jiaji` / 龙邦速递 `longbang` / 速腾物流 `suteng` / 中铁物流 `zhongtie` / 全一快递 `qy`
- 国际表追加 15 行：Ukrposhta（乌克兰）`ukrposhta` / Turkey PTT（土耳其）`turkey-post` / Israel Post（以色列）`israel-post` / Egypt Post（埃及）`egypt-post` / Saudi Post（沙特）`saudi-post` / South African Post（南非）`south-african-post` / Correos de México（墨西哥）`correos-mexico` / Correo Argentino（阿根廷）`correo-argentino` / Correos de Chile（智利）`correos-chile` / Pos Indonesia（印尼）`pos-indonesia` / PHLPost（菲律宾）`phl-post` / Pakistan Post（巴基斯坦）`pakistan-post` / Kazpost（哈萨克斯坦）`kazpost` / Poșta Română（罗马尼亚）`romania-post` / Hrvatska pošta（克罗地亚）`croatia-post`

- [ ] **Step 7: 局部验证**

Run: `vendor/bin/phpunit tests/Unit/DetectorTest.php tests/Unit/CarrierRegistrySmokeTest.php`
Expected: `OK (93 tests, ...)`（DetectorTest 92 + 冒烟 1）

### Task 6: 全量验证 + 提交

- [ ] **Step 1: 全量测试**

Run: `vendor/bin/phpunit`
Expected: `OK (884 tests, ...)`（722 基线 + 147 新适配器 + 15 新检测用例），全绿

- [ ] **Step 2: 提交**

```bash
git add README.md config/logistics.php src/Resources/carrier-registry.php src/Resources/detector-rules.php tests/Unit/CarrierRegistrySmokeTest.php tests/Unit/DetectorTest.php docs/superpowers/plans/2026-08-15-wave6-mixed-21.md src/Carriers/Domestic/ src/Carriers/International/ tests/Carriers/ tests/fixtures/
git commit -m "$(cat <<'EOF'
feat: 接入 21 家新承运商（国内 6 + 国际 15），共 111 家

国内：天地华宇、佳吉快运、龙邦速递、速腾物流、中铁物流、全一快递
国际：Ukrposhta、Turkey PTT、Israel Post、Egypt Post、Saudi Post、
South African Post、Correos de México、Correo Argentino、Correos de Chile、
Pos Indonesia、PHLPost、Pakistan Post、Kazpost、Poșta Română、Hrvatska pošta
EOF
)"
```

- [ ] **Step 3: 验证工作区干净**

Run: `git status --short`
Expected: 空输出（无未提交/未跟踪文件）

## 自检

- 规格覆盖：21 家清单（Task 2/3/4）✓、共享文件注册（Task 5）✓、S10 规则 15 条置于 FedEx 规则前（Step 3）✓、国内 6 家仅显式调用决策（Step 3 注）✓、冒烟 111（Step 5）✓、提交信息（Task 6）✓
- 占位符扫描：无 TBD/TODO；每家适配器/测试/夹具路径精确给出
- 类型一致性：类名/代码/国家码三表（规格、Task 2/3/4、Task 5）逐一核对一致；`{slug}` 与测试类名、fixtures 目录名一致；S10 正则与国家码一致
